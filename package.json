{
  "name": "edn_auto_ext",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-tailwind": "yarn tailwindcss -i ./src/index.css -o ./build/index.css --watch",
    "dev-js": "yarn babel src -d build -w -D --ignore '**/*.css'",
    "build-tailwind": "yarn tailwindcss -i ./src/index.css -o ./build/index.css --minify",
    "build-js": "yarn babel src -d build -D --minified --ignore '**/*.css'",
    "build": "yarn build-js && yarn build-tailwind"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.3.2",
    "@babel/cli": "^7.21.5",
    "@babel/core": "^7.21.8",
    "@babel/preset-env": "^7.21.5"
  }
}
